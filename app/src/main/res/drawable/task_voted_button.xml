<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <!-- 圆角(倒角) -->
    <corners
        android:radius="5dp"/>
    <!-- 边线 -->
    <stroke
        android:width="1dp"
        android:color="@color/text_hui"/>
    <solid android:color="@color/text_hui"/>
</shape>